# Sonify ML Workspace

Training code lives here, separate from the FastAPI application package. Milestone 1 only creates the workspace and reproducible config pattern. Later milestones add executable detector, landmark, generator, evaluation, and export pipelines.
