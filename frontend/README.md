# Sonify Frontend

Vite, React, and TypeScript editor shell for the Sonify meme generator.

```bash
npm install
npm run dev
npm test
npm run build
```

Milestone 1 implements routing, upload validation, workflow state, controls, and a React Konva canvas overlay. Backend-powered upload, detection, preview generation, and export arrive in later milestones.
